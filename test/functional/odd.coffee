require '../config'
describe 'odd',->
#   describe "#[user]", ->
#      describe "::new()", ->
#         it "should initiate member", ->
#         it "should be a object", ->
#         describe "::[method]", ->
#      describe "::new()::$()", ->
#         it "should initiate member", ->
#         it "should be a object", ->
#         describe "::[method_chainable]", ->
