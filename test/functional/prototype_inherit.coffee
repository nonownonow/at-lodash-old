require '../config'
describe 'prototype_inherit',->
#   # delegation
#   describe "user_extended", ->
#      context "two module loaded, 'at' called by user_extended exclud each module __proto__ until 1depth, and merge one __proto__", ->
