require '../config'

describe 'fdd',->
   describe 'normal_user',->
      context 'when @ has main property',->
         it 'should start',->
      context 'when @ has no main property',->

#   describe "::main",->
#      context "when don't have main function", ->
#         beforeEach -> fs.writeFileSync("../index#{extension}",at_without_main_str)
#         afterEach -> fs.writeFileSync("../index#{extension}",at_str)
#         it "should throw error", ->
#            (->require '../').should.throw('should must have main')
#      context "when have main function",->
#         it 'should be a module.exports', ->
#   describe "#[main]", ->
#      it "should has setter", ->
#      it "should create a property ##{@user_at} which eql the property - #[main]", ->
#      describe "static functions which bounded context to #[main]", ->

